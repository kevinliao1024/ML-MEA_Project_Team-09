# Uni-Mol 分子属性预测训练（unimol_tools）
# 使用 pip install -r requirements.txt 安装

# ==========================================
# 核心依赖（必须）
# ==========================================

# PyTorch（请与你的 CUDA 版本匹配，已安装可跳过）
# 查看 CUDA 版本：python -c "import torch; print(torch.version.cuda)"
torch>=2.0.0

# unimol_tools（核心！）
unimol_tools>=0.1.1

# RDKit（分子处理，conda 安装最可靠）
# pip 安装（Python >=3.10 请用 2025.3.x 及以上）：
rdkit>=2025.3.1
# conda 安装（推荐，环境隔离更好）：
# conda install -c conda-forge rdkit

# ==========================================
# HuggingFace 支持（自动下载预训练权重）
# ==========================================
huggingface_hub>=0.20.0

# 如需使用镜像加速：
# export HF_ENDPOINT=https://hf-mirror.com

# ==========================================
# 数据处理
# ==========================================
pandas>=1.3.0
numpy>=1.21.0
lmdb>=0.9
pickle5; python_version<"3.8"

# ==========================================
# 训练 / 评估
# ==========================================
scikit-learn>=1.0.0      # 额外评估指标
tensorboard>=2.8.0        # 训练日志可视化

# ==========================================
# 可选（开发 / 调试）
# ==========================================
jupyter
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.62.0
